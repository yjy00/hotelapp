<%= render "shared/header" %>

一覧
<%= link_to 'フード一覧', foods_path, class: "hall-btn" %>
<%= link_to 'ドリンク一覧', beverages_path, class: "hall-btn" %>
<%= link_to '備品一覧', equipments_path, class: "hall-btn" %>





フード登録
ドリンク登録
備品登録

<%#<% @instances.each do |instance| %
  <% if instance.class == "Post" %>
    <%# Postの表示 %
    <%= instance.user_id %
    <%= instance.content %
    <%= instance.image %
  <% else %>
    <%# Advertisementの表示 %
    <%= instance.company_id %
    <%= instance.content %
    <%= instance.link_url %
  <% end %
<% end %>