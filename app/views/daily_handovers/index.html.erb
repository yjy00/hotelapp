<%= render "shared/header" %>
      <%= link_to '登録する', new_daily_handover_path, class: "basic-menu-btn" %>
      <%# <%= link_to '編集する', edit_daily_handover_path(daily_handover), class: "basic-menu-btn" %> %>
 
 <% @daily_handovers.each do |daily_handover| %>
      <ul class=''>
        <li>
          <div class="">
            <%= daily_handover.created_at %>
            <p class="">今日のリマーク</p>
              <%= daily_handover.daily_remark %>
            <p class="">早番へ</p>
              <%= daily_handover.to_morning %>
            <p class="">遅番へ</p>
              <%= daily_handover.to_evening %>
            <p class="">明日へ</p>
              <%= daily_handover.for_tomorrow %>
          </div>
        </li>
      </ul>
    <% end %>
